{% extends 'base.html.twig' %}

{% block title %}Hygiene{% endblock %}

{% block body %}
    <h1>Hygiene</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hygiene.id }}</td>
            </tr>
            <tr>
                <th>Couche_urine</th>
                <td>{{ hygiene.coucheUrine ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Couche_selle</th>
                <td>{{ hygiene.coucheSelle ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Bain</th>
                <td>{{ hygiene.bain ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hygiene_index') }}">back to list</a>

    <a href="{{ path('hygiene_edit', {'id': hygiene.id}) }}">edit</a>

    {{ include('hygiene/_delete_form.html.twig') }}
{% endblock %}
